const beerUi = require('./ui.js')
const beerApi = require('./api.js')
const store = require('../store.js')

const createBeer = (event) => {

  // beerApi
  //     .newGame()
  //     .then((response) => beerUi.onCreateBeerSuccess(response))
  //     .catch(() => beerUi.onCreateBeerFailure())
}
